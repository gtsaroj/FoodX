import { CardAnalyticsProp } from "../../models/order.model";

export const analyticsData = (data: CardAnalyticsProp[]) => {
    if(!data) throw new Error("data not found");
    try {
         c
    } catch (error) {
        throw new Error("Unable to aggregate card data file: analytics.ts"+error)
    }
}